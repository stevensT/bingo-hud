<Window x:Class="BingoHud.App.HudWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bingo"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        UseLayoutRounding="True"
        WindowStartupLocation="Manual">
    <!-- The readout arrives at 6.6; until then this is only something to drag. -->
    <Border x:Name="Panel" Background="#DD1E1E1E" BorderBrush="Transparent" BorderThickness="1"
            CornerRadius="6" Padding="12,6">
        <TextBlock Text="Bingo" Foreground="White" FontSize="14" />
    </Border>
</Window>
