<Window x:Class="BingoHud.App.HudWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bingo"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight"
        UseLayoutRounding="True"
        WindowStartupLocation="Manual">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </Window.Resources>
    <Border x:Name="Panel" Background="#DD1E1E1E" BorderBrush="Transparent" BorderThickness="1"
            CornerRadius="6" Padding="12,6">
        <!-- One row per readout line: window, percent with its direction, reset. The columns
             are shared so the figures line up whatever the window names are. Rows are built in
             code from what Core decided; nothing here composes a string. -->
        <DockPanel>
            <!-- The overall severity (AC-5), seen without reading which line. Absent while
                 everything is normal, so a bar appearing is itself the signal. -->
            <Border x:Name="Accent" DockPanel.Dock="Left" Width="3" CornerRadius="1.5"
                    Margin="-6,0,8,0" Visibility="Collapsed" />
            <Grid x:Name="Lines">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
            </Grid>
        </DockPanel>
    </Border>
</Window>
